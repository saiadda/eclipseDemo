<apex:page controller="ListViewController">
    <apex:pageBlock >
        <apex:pageBlockTable value="{!leadds}" var="l">
            <apex:column value="{!l.Name}"/>
            <apex:column value="{!l.email}"/>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>